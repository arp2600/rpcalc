lexer:
	Takes the textual input and breaks it into tokens to hand to the parser
	Handled by flex
parser:
	Takes the tokens returned from flex and works out the grammer of the language
interpreter:
	Takes the grammer and creates/executes bytecode
	Requirements:
		Keeps a dictionary of [key]:[bytecode array]
		Takes command codes, executes the command unless remembering a function body, in which case, creates a function body and adds to map
rpcalc:
	The calculator. Does calculator things
	
